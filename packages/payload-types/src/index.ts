/**
 * Placeholder types — overwritten by Payload via `pnpm generate:types`.
 *
 * Do not edit manually after the payload app is scaffolded; run type
 * generation from the repo root instead.
 */

export interface Config {
  auth: Record<string, never>;
  blocks: Record<string, never>;
  collections: Record<string, never>;
  globals: Record<string, never>;
}
