{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"],
      "env": [
        "NEXT_PUBLIC_*",
        "CLERK_*",
        "SENTRY_*",
        "PAYLOAD_*",
        "TMDB_*",
        "CI",
        "NEXT_RUNTIME",
        "PLOTLINE_URL",
        "POSTGRES_URL",
        "NODE_ENV"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^build", "generate:types"]
    },
    "generate:types": {
      "outputs": ["packages/payload-types/src/**"],
      "cache": false
    }
  }
}
